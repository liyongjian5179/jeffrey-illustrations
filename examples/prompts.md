# Prompt Examples

## Shot list only

```text
Use $jeffrey-illustrations 先不要生图。
请分析下面这篇文章，找出 5 个最适合配图的认知锚点。
每张图输出：位置、主题、核心意思、结构类型、Jeffrey/Byte 动作、中文短标注建议。

<article>
```

## AI Agent workflow

```text
Use $jeffrey-illustrations 为“AI Agent 不是聊天窗口，而是一条可观察的工作流”生成一张 16:9 正文配图。
Jeffrey 在桌前调试一条传送带式 Agent 流程，Byte 在旁边递一个写着“观察”的小牌子。
```

## Docker

```text
Use $jeffrey-illustrations 为“把服务装进容器，才方便迁移和复现”生成一张正文配图。
Jeffrey 把一个小服务盒子放进 Docker 小鲸鱼形状的蓝色箱子里，Byte 在旁边贴上“可复现”标签。
```

## Obsidian / PKM

```text
Use $jeffrey-illustrations 为“笔记不是仓库，而是可以持续连接的知识网络”生成一张正文配图。
Jeffrey 正在把几张笔记卡片用线连起来，Byte 举着一个小小的 Obsidian 菱形图标。
```
